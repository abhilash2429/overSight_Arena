# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.gitignore

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment and secrets
.env
.env.*
!.env.example

# Logs and runtime output
*.log
*.out
*.err
logs/

# Jupyter
.ipynb_checkpoints/

# Training artifacts
# NOTE: we DO want to commit reward curves (under assets/plots/) so reviewers
# can see them without re-running training. The merged model weights are
# intentionally NOT committed (size) and NOT shipped in the Space image
# (see .dockerignore). They are regenerated locally by the training notebook.
oversight-arena-grpo/
oversight-arena-trained/
wandb/
runs/
outputs/
checkpoints/
training/oversight-arena-trained/
training/oversight-arena-grpo/
training_log_snapshot.json
training/training_log_snapshot.json

# Build and packaging
build/
dist/
*.egg-info/

# OS/editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

docs/